import React from 'react'

import { IconLogo } from '../'

const config = {
  title: 'Atoms/Icons/Logo',
  component: IconLogo,
}
export default config

export const logo = () => {
  return <IconLogo />
}
